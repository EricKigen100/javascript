//a method is a function belonging to an object
//array and string methods are available in javascript through proptype chaining, where when we create an array of an object or a string or a date, we inherit all properties and methods(functions )  in javascript global array,object,string and date objects

let username ="eric"
console.log(username.toUpperCase()); //ERIC

console.log("victor".charAt(3)); //t
console.log(username.length);

//array, dates and strings methods allows us to manipulate these pieces of data